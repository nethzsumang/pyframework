

class Validator:
	options = {}

	def validate(self, a_data, a_rules, a_options=None):
		pass
		# for index, rule in  a_rules:
		# 	a_rules = rule.split('|')
		# 	for s_rule in a_rules:
		# 		options = s_rule.split(':')
		# 		if len(options) == 1:

